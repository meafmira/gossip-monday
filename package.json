{
    "scripts": {
        "dev": "astro dev",
        "build": "astro build",
        "preview": "astro preview",
        "deploy": "npm run build && npx gh-pages -d dist -b gh-pages -t --add"
    },
    "dependencies": {
        "@astrojs/check": "0.9.8",
        "astro": "6.2.1",
        "typescript": "5.9.3"
    }
}
